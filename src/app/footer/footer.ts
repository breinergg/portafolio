import { Component } from '@angular/core';
import { Reveal } from '../shared/reveal';

@Component({
  selector: 'app-footer',
  imports: [Reveal],
  templateUrl: './footer.html',
  styleUrl: './footer.css',
})
export class Footer {}
